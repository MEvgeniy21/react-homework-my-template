import styled from '@emotion/styled';
import {
  typography,
  space,
  color,
  layout,
  flexbox,
  border,
  position,
  grid,
} from 'styled-system';

export const Box = styled('div')(
  typography,
  space,
  color,
  layout,
  flexbox,
  border,
  position,
  grid
);
